# Encrypt this file with SOPS before committing:
#   sops --encrypt --in-place --age <YOUR_AGE_PUBLIC_KEY> helm/example-service/values-secret.yaml
# or (GPG):
#   sops --encrypt --in-place --pgp <YOUR_GPG_FINGERPRINT> helm/example-service/values-secret.yaml
# If you use .sops.yaml creation rules, `sops --encrypt --in-place` is enough.

secrets:
  SQL_PASSWORD: "replace-me"
  REDIS_PASSWORD: "replace-me"
  INTEGRATIONS_API_TOKEN: "replace-me"
  KAFKA_SASL_USERNAME: ""
  KAFKA_SASL_PASSWORD: ""
